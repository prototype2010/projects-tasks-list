import React from 'react';
import { MainLayoutContent } from 'components/Layout/styled';

const MainLayout: React.FC = ({ children }) => <MainLayoutContent>{children}</MainLayoutContent>;

export default MainLayout;
